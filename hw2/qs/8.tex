Let $\mathsf{V}$ be an $n$-dimensional vector space with an ordered
basis $\beta$. Define $\mathsf{T}\colon\mathsf{V}\to\mathsf{F}^n$ by
$\mathsf{T}(x) = \left[x\right]_\beta$. Prove that $\mathsf{T}$ is linear.
